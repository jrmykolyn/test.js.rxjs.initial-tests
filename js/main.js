function logData( data ) {
    console.log( 'LOGGING OUT STREAM DATA' );
    console.log( data );
}

function logError( error ) {
    console.error( 'LOGGING OUT `error`' );
    console.error( error );
}

 function logComplete() {
    console.log( 'STREAM COMPLETE' );
 }